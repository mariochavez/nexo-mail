# Secrets — Gmail App Password, API keys, etc. NEVER commit.
.env
.env.*

# The real runtime sandbox lives under $XDG_STATE_HOME; this only catches a
# stale local copy from earlier dev runs.
/sandbox/

# Ruby / bundler / gem build
Gemfile.lock
/.bundle/
/vendor/bundle/
/pkg/
*.gem
.DS_Store
tmp/
