# frozen_string_literal: true

source "https://rubygems.org"

# Dependencies are declared in nexo_mail.gemspec.
gemspec

group :development do
  gem "rake"
  gem "standard" # linter + formatter (github.com/standardrb/standard)
end
